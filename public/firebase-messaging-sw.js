/*
  Firebase Cloud Messaging service worker.
  This file runs in the background and can display notifications when a push message is received.
  It is registered automatically when `registerPushNotifications()` is called in the app.

  Note: To send messages to this device, you need to send FCM push messages (via server key) to the
  FCM token generated by the client. That requires a backend service.
*/

importScripts('https://www.gstatic.com/firebasejs/12.8.0/firebase-app-compat.js');
importScripts('https://www.gstatic.com/firebasejs/12.8.0/firebase-messaging-compat.js');

// This should match your Firebase config in Store.tsx
const firebaseConfig = {
  apiKey: "AIzaSyBryZdhGRp05tY9e6gv79z4mA1hDyXlQ8U",
  authDomain: "expensetracker-pro-35f24.firebaseapp.com",
  projectId: "expensetracker-pro-35f24",
  storageBucket: "expensetracker-pro-35f24.firebasestorage.app",
  messagingSenderId: "360942386894",
  appId: "1:360942386894:web:61043d1a8ac7a65f64da9f",
  measurementId: "G-PXC2T5SKGB"
};

firebase.initializeApp(firebaseConfig);

const messaging = firebase.messaging();

messaging.onBackgroundMessage((payload) => {
  console.log('[firebase-messaging-sw.js] Received background message', payload);
  const notification = payload.notification || {};
  const title = notification.title || 'Expense Tracker';
  const options = {
    body: notification.body || payload.data?.body || '',
    icon: '/icon.png',
  };

  self.registration.showNotification(title, options);
});
